<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blueMarine</item>
        <item name="colorPrimaryDark">@color/blueMarine</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CustomToolBarStyle" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/lightWhite</item>
        <item name="android:background">@color/blueMarine</item>
        <item name="android:color">@color/lightWhite</item>
    </style>

</resources>
